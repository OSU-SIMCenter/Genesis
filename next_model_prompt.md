# PROMPT FOR NEXT MODEL

You are continuing the implementation of Thermal Physics for the Genesis MPM Solver.

## 1. Context Analysis
**Read these files first:**
1.  `handover_v3.md`: The master status doc. (Refactoring is DONE. Focus on Heat features).
2.  `genesis/engine/solvers/mpm_solver.py`: The derived class (Thermal ext).
3.  `genesis/engine/solvers/base_mpm_solver.py`: The base class.

## 2. Your Task
**Primary Objective**: Implement **Plastic Work Heating** (Adiabatic Heating).

1.  **Understand the Architecture**: `MPMSolver` extends `BaseMPMSolver` and uses hooks (`modify_stress`, `transfer_extra_fields`) to inject logic.
    - *Do NOT* revert to copying entire kernels. Keep using the hooks.
2.  **Implement Plastic Work Heating**:
    - You need to calculate the heat generated by plastic deformation ($W_p = \sigma : \dot{\epsilon}_p \Delta t$).
    - Update `p2g_helper` or the `p2g_modify_stress` hook to capture this energy and add it to `particles.temp`.
    - *challenge*: `Material.update_stress` might not return plastic strain. You may need to inspect/refactor `genesis/options/materials.py` or the update logic to get this value.

## 3. Important Context (From User)
- "The regular/new mpm_solver should always have temperature/thermal modeling."
- "Visual/Aesthetic excellence is important".

## 4. Constraint
- **Do not** break the existing Contact Cooling (`grid_op_thermal`).
- **Do not** duplicate kernels. Maintain the hook-based architecture.

Good luck.
